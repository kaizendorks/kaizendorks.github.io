// Inherit from the default theme-blog. See https://vuepress.vuejs.org/theme/inheritance.html#motivation
// Main driver is being able to add the logo to the header, so we override the Header component
module.exports = {
  extend: '@vuepress/theme-blog'
}